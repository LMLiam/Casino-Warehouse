# Completion Audit

## Objective Restated

Finish the per-game multiplayer product, not only the protocol: normal play must use a selected-game room browser; Blackjack must be a real five-seat shared-dealer table; Slots must use per-player wagers with shared outcomes; generated PNG assets must come through the manifest/config; docs must match the protocol; behavioural tests must cover the UX, multiplayer state, tablet layout, and animations; build artefacts must be ignored; and the full quality gate must pass.

## Prompt-to-Artifact Checklist

| Requirement                               | Evidence                                                                                                                                                                                                                                                         | Status   |
| ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| Proper room browser UX                    | `src/app/views/RoomBrowserView.ts` renders Game Rooms with Create Room, Refresh Rooms, and per-room Join Player/Spectate actions; old server input, Connect button, raw room id input, and generic join buttons were removed from the template.                  | Verified |
| Browse rooms by selected game             | `refreshMultiplayerRooms()` sends `list-rooms` for `this.activeGame`; `RoomAuthority.listRoomSummaries(gameId)` filters by exact game id; E2E `room browser UX uses per-game cards...` verifies the normal UI surface.                                           | Verified |
| Create room for selected game             | `hostMultiplayerRoom()` creates with `this.activeGame`; `defaultRoomMaxPlayers()` sets 3 Beat the House, 5 Blackjack, 4 Slots defaults.                                                                                                                          | Verified |
| Join/spectate from room list              | `renderRoomBrowser()` creates room cards with `data-room-join` and `data-room-spectate` buttons wired to `joinMultiplayerRoom(roomId, role)`.                                                                                                                    | Verified |
| Leave cleanly to room list/lobby          | `leaveMultiplayerRoom()` sends `leave-room`, clears local room state, refreshes the selected game room list, and rerenders.                                                                                                                                      | Verified |
| Direct invite links remain                | `applyInviteFromUrl()` stores hidden room/server URL state; `maybeAutoJoinInvite()` joins after profile selection without visible manual room controls.                                                                                                          | Verified |
| True five-seat Blackjack                  | `src/game/blackjackTable.ts` implements `BlackjackTable` with five independent seat snapshots, wagers, hands, turns, settlements, and one shared dealer. `RoomAuthority` uses this table for Blackjack rooms.                                                    | Verified |
| Blackjack server turn ownership           | `BlackjackTable.act()` requires the active seat; `RoomAuthority.blackjackAction()` maps profile to seat and rejects spectators. Unit tests assert wrong-turn and spectator rejection.                                                                            | Verified |
| Blackjack independent settlement/bankroll | `applyBlackjackSettlements()` pays each profile by seat; `tests/multiplayer-room.test.ts` verifies five distinct wagers, shared dealer cards, independent seat hands, and settlement profiles.                                                                   | Verified |
| Slots per-player wagers                   | Slot room model uses `wagersByProfileId`; snapshot exposes per-profile wagers; `setSlotsWager()` updates only the calling profile and clears that profile's ready state.                                                                                         | Verified |
| Slots ready/shared-spin flow              | `setSlotsReady()` requires a wager; `spinSlots()` refuses to spin until all active players are ready, clears ready state after a spin, and no client shortcut sends wager+ready+spin together.                                                                   | Verified |
| Slots independent settlement              | `settleSlots()` emits one settlement per profile using that profile's wager and the shared outcome ratio; tests verify distinct wagered amounts and profile ids.                                                                                                 | Verified |
| Duplicate/rapid Slots protection          | Ready state clears after spin; duplicate spins without all players ready are rejected; `settledSpinKeys` prevents duplicate settlement for the same spin key.                                                                                                    | Verified |
| Generated PNG asset usage                 | `blackjackTableAsset()` and `slotFrameAsset()` from `src/assets/manifest.ts` feed CSS variables in `GameApp.ts`; CSS no longer hardcodes `/assets/blackjack/table.png`.                                                                                          | Verified |
| No placeholder/vector asset fallback      | `tests/assets-manifest.test.ts` verifies public assets match manifest paths, dimensions, no SVG files remain, and stale root asset paths are absent.                                                                                                             | Verified |
| Protocol docs current                     | README lists `list-rooms`, Blackjack and Slots messages, `room-list`, and no longer documents `select-game` as valid.                                                                                                                                            | Verified |
| Room browser E2E                          | `tests/e2e/casino-visual.spec.ts` verifies manual controls are absent and room-browser controls are visible.                                                                                                                                                     | Verified |
| Multiplayer Blackjack tests               | `tests/multiplayer-room.test.ts` includes true five-seat shared-dealer Blackjack coverage with independent wagers and settlements.                                                                                                                               | Verified |
| Multiplayer Slots tests                   | `tests/multiplayer-room.test.ts` includes per-player wager, ready, shared-spin, settlement, and duplicate-spin rejection coverage.                                                                                                                               | Verified |
| Tablet layout stability                   | `tests/e2e/casino-visual.spec.ts` opens the multiplayer panel at tablet size and compares table canvas bounds before/after.                                                                                                                                      | Verified |
| Popup/animation behaviour                 | `PixiTable` exposes rendered settlement/dealer animation state; Playwright drives a Beat the House round and verifies settlement popups persist until Next Round and dealer reveal state is rendered.                                                            | Verified |
| Repo hygiene                              | `.gitignore` now ignores `node_modules/`, `coverage/`, `dist/`, `dist-server/`, `.idea/`, Playwright reports, test results, logs, and `.DS_Store`.                                                                                                               | Verified |
| Full quality gate                         | `npm run check` passed after this audit update. It ran lint/typecheck, Prettier check, coverage, server build, client build, and Playwright. Playwright reported 18 passed and 2 optional public-tunnel smoke skips because `PUBLIC_TUNNEL_SMOKE_URL` was unset. | Verified |

## Verification Log

Final command results from this pass:

- `npm run typecheck`: passed.
- `npm test`: passed, 10 files and 71 tests before the deterministic BlackjackTable coverage expansion.
- `npm run visual`: passed, 18 browser tests and 2 skipped optional public tunnel smoke tests.
- `npm run test:coverage`: passed after the deterministic BlackjackTable coverage expansion, 10 files and 76 tests. Coverage summary: statements 90.14%, branches 80.99%, functions 96.28%, lines 89.7%.
- `npm run check`: passed. This included ESLint, TypeScript typecheck, Prettier check, Vitest coverage, server build, client build, and Playwright visual/e2e. Playwright result: 18 passed and 2 skipped because `PUBLIC_TUNNEL_SMOKE_URL` is intentionally required for the public tunnel smoke inside the normal gate.
